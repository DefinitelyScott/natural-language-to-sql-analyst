name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install ruff
        # Pinned so a new ruff release adding rules cannot break CI on an
        # unrelated commit; bump deliberately alongside any needed fixes.
        run: pip install ruff==0.16.2
      - name: Lint
        run: ruff check .

  types:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          # The oldest interpreter the project supports, matching
          # `python_version` in the mypy config: checking against the floor is
          # what catches a syntax or typing feature that only exists on newer
          # versions.
          python-version: "3.10"
      - name: Install mypy
        # Pinned for the same reason ruff is — a new release adding checks
        # would otherwise fail CI on a commit that changed nothing. fastapi and
        # pydantic ship inline types, so installing them is what lets
        # `nl2sql/api.py` be checked for real rather than exempted the way
        # `openai` is; an exemption would reduce every decorated endpoint to
        # `Any` and check nothing.
        run: pip install mypy==2.3.1 "fastapi>=0.110"
      - name: Type check
        # Paths, strictness and the `openai` exemption all live in
        # pyproject.toml, so this runs the same check a contributor gets from a
        # bare `mypy`. `openai` is deliberately not installed here: the offline
        # path must type-check without it.
        run: mypy

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        # The API extra is installed here so `tests/test_api.py` actually runs.
        # It guards itself with `importorskip`, which means the alternative is
        # not a failing job but a silently skipped one — an HTTP surface nobody
        # tests, reported as green.
        run: pip install pytest "fastapi>=0.110" "httpx>=0.27"
      - name: Build sample database
        run: python scripts/build_sample_db.py
      - name: Run tests
        run: pytest -q
      - name: Run evaluation harness (offline backend)
        run: python evals/evaluate.py
